node_modules/
.smoketest/
.smoketest-*/
.ci-test/
.ci-safety/
*.log
.DS_Store
.env
.env.local

# Generated stubs — kit/ is the source of truth, .claude/ is regenerated by `sync install`
.claude/agents/
.claude/commands/
.claude/skills/
.claude/framework/
.claude/hooks/
.claude/settings.local.json
CLAUDE.md

# Phase 99 (OBS-19-01): persistSnapshot writes rolling JSON snapshots here.
# Dev-only artifact — process-local metrics state, not a shipped doc.
# Cleanup is automatic (>30d) via cleanupOldSnapshots in src/core/metrics.js.
.planning/metrics/snapshots/

# Phase 101 (INFRA-20-02): Stryker mutation testing artifacts.
# Reports são local-only — execução opt-in via `npm run test:mutation`.
# Baseline documentado em .planning/audits/v1.20/MUTATION-BASELINE.md.
reports/mutation/
.stryker-tmp/


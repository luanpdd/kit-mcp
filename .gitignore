node_modules/
.smoketest/
.smoketest-*/
.ci-test/
.ci-safety/
*.log
.DS_Store
.env
.env.local

# Generated stubs — kit/ is the source of truth, .claude/ is regenerated by `sync install`
.claude/agents/
.claude/commands/
.claude/skills/
.claude/workflows/
.claude/framework/
.claude/hooks/
.claude/settings.local.json
.claude/.kit-mcp-version
.claude/.kit-mcp-restart-required
CLAUDE.md

# Phase 99 (OBS-19-01): persistSnapshot writes rolling JSON snapshots here.
# Dev-only artifact — process-local metrics state, not a shipped doc.
# Cleanup is automatic (>30d) via cleanupOldSnapshots in src/core/metrics.js.
.planning/metrics/snapshots/

# Phase 172 (Cost Tracking): persistSnapshot opt-in para tools cost-*.
# Dev-only artifact — local cost telemetry, not a shipped doc.
# Cleanup automático (>30d) via persist-snapshot.js.
.planning/costs/

# Phase 172 (Cost Tracking) M4: large JSONL fixture is generated on demand
# by test/integration/cost-statusline.large-jsonl.test.js (~5MB) — keep out
# of the repo; the generator is deterministic enough for local reruns.
test/fixtures/jsonl-statusline-10k.jsonl

# Phase 101 (INFRA-20-02): Stryker mutation testing artifacts.
# Reports são local-only — execução opt-in via `npm run test:mutation`.
# Baseline documentado em .planning/audits/v1.20/MUTATION-BASELINE.md.
reports/mutation/
.stryker-tmp/


---
plan: 105-01
phase: 105-prr-performance-5-5
wave: 1
depends_on: []
files_modified:
  - src/mcp-server/index.js (add pre-warm listKit after server.connect)
  - test/unit/mcp-server-prewarm.test.js (NEW — regression test for pre-warm trigger)
  - .planning/BENCHMARK.md (extend with v1.20.0 M4 measurement row)
  - .planning/audits/v1.20/PRR-RECHECK.md (update Performance 4/5 → 5/5)
autonomous: true
gap_closure: false
requirements: [SRE-20-02]
must_haves:
  - "MCP server pre-warms listKit cache after server.connect (fire-and-forget)"
  - "Regression test verifies pre-warm path is reachable + non-blocking"
  - "BENCHMARK.md M4 re-measured with new p95 sub-100ms target"
  - "PRR-RECHECK.md updates Performance 4/5 → 5/5 with evidence"
  - "Stable API v1.0+ preserved (no new exports, signature unchanged)"
  - "Suite all-green pre/post"
  - "If sub-100ms NOT achieved, document trade-off + fallback decision (still defensible 5/5)"
---

# Plano 105-01: PRR Performance 4/5 → 5/5 — kit cache pre-warm + benchmark

## Objetivo

Elevar PRR Performance axe 4/5 → 5/5 via warm-up estratégico do kit cache no boot MCP server: invocar `listKit(BUNDLED_KIT_ROOT)` fire-and-forget após `server.connect()`, eliminando cold-path do primeiro dispatch. Re-medir M4 com N=30 — esperado p95 < 100ms (vs 144ms baseline). Update BENCHMARK.md + PRR-RECHECK.md.

## Tasks

### Task 1: Add pre-warm in src/mcp-server/index.js

<read_first>
- src/mcp-server/index.js (find server.connect call site, near end of module)
- src/core/kit.js (find BUNDLED_KIT_ROOT export + listKit signature + kitCache pattern)
- test/unit/mcp-error-envelope.test.js (canonical pattern for MCP server invocation in tests)
</read_first>

<action>
1. **Locate** the server.connect block in `src/mcp-server/index.js` (typically near end of module, look for `await server.connect(transport)` or `await server.connect(...)` pattern).

2. **Verify** `BUNDLED_KIT_ROOT` is exported from `src/core/kit.js`:
   ```bash
   grep -n "BUNDLED_KIT_ROOT" src/core/kit.js
   ```
   If not exported, may need to import via path computation (e.g. `path.resolve(__dirname, '../../kit')`).

3. **Add pre-warm call** AFTER `server.connect()` but BEFORE module exit. Pattern:
   ```js
   await server.connect(transport);

   // SRE-20-02 (Phase 105): pre-warm kit cache to push MCP dispatch p95 < 100ms.
   // Fire-and-forget — failure here is non-fatal (next dispatch will lazy-populate).
   // This shifts ~144ms cold-path work from the first user-visible request to
   // the boot path where it's invisible behind IDE startup.
   listKit(BUNDLED_KIT_ROOT).catch(() => {});
   ```

4. **Comment** must reference SRE-20-02 + Phase 105 + skill `production-readiness-review`.

5. **Stable API:** zero new exports, signature unchanged. Internal side-effect only.
</action>

<acceptance_criteria>
- `grep -n "SRE-20-02" src/mcp-server/index.js` returns ≥ 1 hit
- `grep -n "listKit(BUNDLED_KIT_ROOT)" src/mcp-server/index.js` returns ≥ 1 hit (the pre-warm call)
- `grep -n "Phase 105" src/mcp-server/index.js` returns ≥ 1 hit (commenting attribution)
- `node -e "import('./src/mcp-server/index.js').then(() => console.log('OK'))"` exits 0 (no syntax error)
- `git diff src/mcp-server/index.js` shows ONLY pre-warm addition (no signature/return shape changes)
</acceptance_criteria>

### Task 2: Create test/unit/mcp-server-prewarm.test.js

<read_first>
- test/unit/mcp-error-envelope.test.js (MCP boot test pattern — spawn or in-process)
- test/unit/mcp-server-paths.test.js (handler invocation pattern)
- src/mcp-server/index.js (post-Task 1 — pre-warm call site)
</read_first>

<action>
Create `test/unit/mcp-server-prewarm.test.js` with ≥ 2 regression tests:

1. **Test: pre-warm call is reachable** — spawn or in-process import the MCP server module, verify that `listKit` was called shortly after boot (e.g., kitCache populated within 500ms of server.connect resolving).

2. **Test: pre-warm failure is non-fatal** — mock `listKit` to throw, verify server still boots and responds to subsequent requests normally (graceful catch).

Test pattern: use mcp-error-envelope.test.js spawn pattern OR in-process import with mock kit.js.

Strategy:
- (a) Spawn approach: launch `node bin/mcp.js`, send tools/call request, verify p95 latency on N=10 dispatches is below cold-path baseline. Slower to write, but more realistic.
- (b) Mock approach: monkey-patch listKit before import to be a tracked function, verify it's called once shortly after import.

Recommended: (b) since it's faster + sufficient for regression. Edge: ESM imports cache modules, so mock must happen BEFORE first import. Use `import.meta.resolve` + dynamic mock OR simpler: just verify `kitCache` populated after module load (proxy signal).

Even simpler: black-box test via spawn — drive 10 sequential `kit:list-agents` requests, assert p95 < 100ms (was 144ms). Robust if pre-warm works.
</action>

<acceptance_criteria>
- `test/unit/mcp-server-prewarm.test.js` exists with ≥ 2 tests
- `node test/run.mjs test/unit/mcp-server-prewarm.test.js` exits 0
- At least one test verifies the pre-warm call site is reachable
- At least one test verifies graceful failure (pre-warm error doesn't kill server)
</acceptance_criteria>

### Task 3: Re-measure M4 + update BENCHMARK.md

<read_first>
- .planning/BENCHMARK.md (current M4 with p95=144.55ms)
- test/integration/mcp-metrics-snapshot.test.js (canonical N=30 probe pattern)
</read_first>

<action>
1. **Run probe** N=30 dispatches against fresh server with pre-warm enabled:
   ```bash
   node test/integration/mcp-metrics-snapshot.test.js
   # Capture p95/p99/count from output
   ```

2. **Capture numbers** — record p50, p95, p99, count from actual run.

3. **Update `.planning/BENCHMARK.md` M4 section** preserving history pattern (`# v1.17.0` row vs `# v1.20.0`):
   ```
   ### M4 — MCP tool dispatch latency (in-server, p50/p95/p99)

   #### v1.20.0 — pre-warm kit cache (Phase 105)

   | Tool | Count | p50 | p95 | p99 |
   |---|---|---|---|---|
   | `kit` | 30 | <new p50> | <new p95> | <new p99> |

   **Reading:** Phase 105 added pre-warm of listKit at boot. p95 dropped from 144ms → <new>ms (Δ=-X%). The cold-path work is now shifted to boot, invisible behind IDE startup.

   #### v1.17.0 — pre-warm absent (baseline) [archived]
   <existing table>
   ```

4. **Update Summary table** at bottom of BENCHMARK.md to reflect new headline number for M4 p95.

5. If new p95 ≥ 100ms (didn't hit sub-100ms target):
   - Document trade-off in BENCHMARK.md: pre-warm reduced p95 by X% but didn't reach sub-100ms due to <reason>.
   - Document fallback path: blocking await (would add boot delay).
   - PRR Performance can still go 4/5 → 5/5 if reduction ≥ 30% (per ROADMAP "≥30% redução vs 144ms baseline").
</action>

<acceptance_criteria>
- `.planning/BENCHMARK.md` has v1.20.0 M4 section with measured numbers
- p95 documented (target sub-100ms; minimum acceptable: ≥ 30% reduction = ≤ 101ms)
- Summary table updated with new headline
- v1.17.0 row preserved as historical [archived]
</acceptance_criteria>

### Task 4: Update PRR-RECHECK.md + closure

<read_first>
- .planning/audits/v1.20/PRR-RECHECK.md (created in Phase 104; update Performance row)
- .planning/BENCHMARK.md (post-Task 3)
</read_first>

<action>
1. **Update `.planning/audits/v1.20/PRR-RECHECK.md`** Performance row:
   ```
   | **Performance** | 4/5 | **5/5** | **+1** | Phase 105 — pre-warm kit cache; M4 p95 dropped <X%> (BENCHMARK.md v1.20.0) |
   ```
   - Add new section "Performance Axe — 4/5 → 5/5 justification" mirroring the Emergency justification format
   - Update Total: 28/30 → **30/30**

2. **Suite green check**: `node test/run.mjs test/unit` — verify suite still all-green.

3. **Atomic commits:**
   - Commit 1: `feat(105-01): pre-warm kit cache after MCP server.connect (SRE-20-02)`
   - Commit 2: `test(105-01): regression for pre-warm reachability + graceful failure`
   - Commit 3: `docs(105-01): BENCHMARK.md v1.20.0 — M4 p95 <X>ms (vs 144ms baseline)`
   - Commit 4: `audit(105-01): PRR-RECHECK.md — Performance 4/5 → 5/5; total 30/30`
   - Commit 5: `docs(105): SUMMARY + STATE/ROADMAP — Phase 105 closure`

4. **Update STATE.md** + ROADMAP.md via roadmap update-plan-progress 105 105-01 complete.

5. **Create SUMMARY.md** documenting:
   - Code change (pre-warm one-liner)
   - Measurement before/after (p95 old vs new)
   - PRR delta (Performance 4 → 5; total 28 → 30/30)
   - Stable API preservation
   - Self-Check: PASSED
</action>

<acceptance_criteria>
- PRR-RECHECK.md Performance row shows 4/5 → 5/5
- Total row shows 30/30
- 5 atomic commits
- SUMMARY.md created
- STATE.md + ROADMAP.md updated
- Suite all-green
- Stable API v1.0+ preserved
</acceptance_criteria>

## Verification Criteria

- [ ] MCP server pre-warms listKit after server.connect
- [ ] Regression test for pre-warm reachability + graceful failure
- [ ] BENCHMARK.md updated with v1.20.0 M4 row (p95 < 100ms or ≥ 30% reduction)
- [ ] PRR-RECHECK.md Performance 4/5 → 5/5 (total 30/30)
- [ ] Suite all-green
- [ ] Stable API v1.0+ preserved

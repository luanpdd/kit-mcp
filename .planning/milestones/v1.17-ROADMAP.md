# v1.17 ROADMAP — Performance Wave 2 + Quick Wins

> Primeira release pós-zerada-meta-auditoria-original. Origem: nova meta-auditoria (5 agents) sobre v1.16.0. Score PRR projetado: 22 → 24/30.

**Milestone:** v1.17
**Total de fases:** 4 (Fases 90-93)
**Estrutura:** 2 fases P0 perf (90-91) + 1 polish (92) + 1 infra CI (93)
**Criado:** 2026-05-09 (após v1.16.0 publicado)
**Origem:** `.planning/audits/v1.16/AUDIT-SYNTHESIS.md` + `PRR-REPORT.md`.

---

## Visão geral

A meta-auditoria pós-v1.16 (5 agents paralelos) confirmou wins reportados (50.5%/52.4%) e descobriu 2 novos P0 hotspots:
1. **verifyManifest sequencial** (47% do syncTo é gasto antes do batched loop) — Phase 90 paraleliza + cache
2. **Sync sem diff** (re-sync escreve todos files mesmo sem mudanças) — Phase 91 stat-based diff

Phase 92 captura polish items (4 quick wins). Phase 93 fecha 2 gaps de CI infrastructure.

---

## Fase 90: verifyManifest Parallel + Cache

**REQs:** PERF-17-01

**Files:**
- `src/core/manifest-verify.js` — Promise.all batches=16 + cache TTL 30s
- Tests novos para parallel + cache invalidation

**Risco:** baixo. Pattern (Promise.all batch=16) já validado em Phase 88.01. Cache é aditivo. CRLF→LF normalize fix preservado.

---

## Fase 91: Diff-Based Sync

**REQs:** PERF-17-02

**Depends on:** Phase 90

**Files:**
- `src/core/sync.js` — stat-based diff antes do batch loop
- Env opt-out `KIT_MCP_FORCE_FULL_SYNC=1`
- Tests novos

**Risco:** médio. mtime pode ser stale em raros casos (touch sem write); env opt-out cobre. Phase 88.01 Promise.all preservado.

---

## Fase 92: Quick Wins Polish ✅

**REQs:** POL-17-01..04 (todos concluídos 2026-05-09)

**Depends on:** Phase 90

**Files entregues:**
- `package.json` — `open` para optionalDependencies (3 deps + 3 opt = 6 total mantido) ✅
- `src/ui/browser.js` — fallback graceful (já existia desde Phase 89; só test contract atualizado) ✅
- `scripts/regen-manifest.js` — Promise.all hash batches=16 (~37% speedup, 86ms→54ms) ✅
- `src/cli/index.js` — `getLocalVersion` import morto removido ✅
- `src/core/path-safety.js` — JSDoc com @param/@returns em validateProjectRoot ✅

**Tests novos:** 7 (1 dep budget atualizado, 1 openBrowser fallback, 1 parallel-hash ordering, 3 dead-imports, 2 jsdoc-coverage; +regression baseline em error-redaction.js)
**Commits:** `37dc148` (POL-17-01), `6dfd8ec` (POL-17-02), `83ab50a` (POL-17-03), `fada87a` (POL-17-04)
**Status:** Concluída 2026-05-09 (~7min, plan+execute combinado).

---

## Fase 93: CI Deps Gate + Coverage Tooling

**REQs:** INFRA-17-01..02

**Depends on:** Phase 92 (após budget mudar para 3+3=6 mantido)

**Files:**
- `.github/workflows/ci.yml` — gate dependencies+optionalDependencies + coverage step

**Risco:** baixo-médio. Coverage step precisa threshold realista (75%); pode revelar gaps que precisam test patches.

---

## Notas

- **Stable API v1.0+ preservada.** Budget 6 deps mantido (3 dependencies + 3 optionalDependencies pós-Phase 92).
- **Suite test esperado pós-v1.17:** ~330 (317 + ~13 testes novos).
- **Após v1.17:** PRR projetado 24/30 (Performance 4→5; Instrumentation continua 2/5 fail axe — endereçado em v1.18+ se houver).

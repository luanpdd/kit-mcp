# Phase 122 — Plan 01: 3 Agents + Comando `/dados-distribuidos`

**Status:** in-progress
**Modo:** Materialização direta (autonomo workflow content-only)

## Objetivo

Materializar **3 agents** + **1 comando orquestrador** da Suíte DDIA Foundations v1.22:

1. `auditor-consistencia-isolamento` — auditor de race conditions (6 detectores canônicos)
2. `detector-tenant-quente` — detector de outliers de tenant via logs Supabase
3. `validador-evolucao-schema` — validador GO/NO-GO de breaks em schema evolution
4. `/dados-distribuidos` — comando orquestrador único da Suíte DDIA Foundations (4 subcomandos)

Total: 8 REQs cobertos em 1 plano (cobertura agrupada — 3 agents + 1 comando).

## Tarefas

| # | Tarefa | Status | Artefato |
|---|---|---|---|
| 1 | Ler templates estruturais (`multi-tenant-isolation-auditor.md`, `toil-auditor.md`, `schema-checker.md`, `multi-tenant.md`) | done | n/a |
| 2 | Verificar existência das 7 skills da v1.22 (cross-refs ATIVOS) | done | n/a |
| 3 | Escrever `kit/agents/auditor-consistencia-isolamento.md` cobrindo 6 detectores com SQL/grep real | in-progress | agent 1 |
| 4 | Escrever `kit/agents/detector-tenant-quente.md` com 3 métricas SQL via mcp__supabase | in-progress | agent 2 |
| 5 | Escrever `kit/agents/validador-evolucao-schema.md` com 4 detectores + veredito GO/NO-GO | in-progress | agent 3 |
| 6 | Escrever `kit/commands/dados-distribuidos.md` com 4 subcomandos + sinônimos PT/EN | in-progress | command 1 |
| 7 | Cross-refs ATIVOS em todos: skills v1.22 + agents v1.21 + agents v1.8 | in-progress | embedded |
| 8 | 5 commits atômicos via `tools.cjs commit` (CONTEXT+PLAN, agent 1, agents 2+3, command, SUMMARY+VERIFICATION) | in-progress | git |

## REQs cobertos

### Suíte (SUITE-01, SUITE-02)

- **SUITE-01:** Comando `/dados-distribuidos` com 4 subcomandos canônicos (`auditar-consistencia`, `auditar-tenant-quente`, `validar-evolucao-schema`, `implementar-cdc`) + sinônimos PT/EN + dispatch via `Task(subagent_type=...)` + fallback amigável.
- **SUITE-02:** Cross-suite invocation pattern documentado em todos os 3 agents (delegam para `supabase-migration-writer` v1.8 + `supabase-edge-fn-writer` v1.8 sem reimplementar).

### Agents (AGENTE-01..06)

- **AGENTE-01:** Agent `auditor-consistencia-isolamento` com 6 detectores canônicos de race condition (SELECT-then-UPDATE sem `FOR UPDATE` = lost update P0; trigger sem materializar predicate = write skew P1; `now()`/`clock_timestamp()` em expiração = clock skew P0; UNIQUE check em app = race P0; write cross-tenant sem lock = lost update P1; handler sem idempotência = duplicate processing P1).
- **AGENTE-02:** Output `AUDITORIA-CONSISTENCIA.md` priorizado P0/P1/P2 com findings linkados a `arquivo:linha` + sugestão de fix referenciando skill canônica via Markdown link relativo.
- **AGENTE-03:** Agent `detector-tenant-quente` consulta logs Supabase via `mcp__supabase__execute_sql` (queries últimos 30d agrupadas por org_id), aplica thresholds 3×/10× P50 da skill TENANT.
- **AGENTE-04:** Output `AUDITORIA-TENANT-QUENTE.md` com top 5 tenants quentes + 3 métricas (queries/min, storage GB, conexões) + estratégia sugerida (cross-ref ATIVO para `tenant-quente-mitigacao`).
- **AGENTE-05:** Agent `validador-evolucao-schema` recebe SQL via stdin/argument, detecta 4 breaks canônicos (NOT NULL added, column dropped, type narrowed, default changed) + veredito GO/NO-GO/NEEDS-REVIEW + 3-step migration safe sugerida quando NO-GO.
- **AGENTE-06:** Todos os 3 agents documentam cross-suite invocation explicitamente — delegam escrita de migration corrigida para `supabase-migration-writer` v1.8 + Edge Function corrigida para `supabase-edge-fn-writer` v1.8 (pattern v1.21 herdado).

## Convenção de conteúdo

- Frontmatter `name` + `description` PT-BR
- Headings PT-BR
- Narrativa PT-BR
- Code blocks SQL/JS em EN (sintaxe Postgres/Deno) com comentários PT-BR
- Termos técnicos canônicos preservados: `lost update`, `write skew`, `clock skew`, `phantom read`, `idempotência` (PT-BR canônico), `dedup table`, `outbox`, `pgmq`, `pg_stat_statements`, `pg_total_relation_size`, `mcp__supabase__execute_sql` (não traduzir)
- Cross-refs ATIVOS via Markdown link relativo (`../skills/<name>/SKILL.md`, `./<agent-name>.md`)
- Agents fazem dispatch via `Task()` — NÃO duplicam lógica de outras suítes
- Zero alteração em `src/core/`
</content>

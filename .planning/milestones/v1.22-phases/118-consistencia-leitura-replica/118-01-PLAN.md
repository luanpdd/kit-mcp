# Phase 118 — Plan 01: Skill `consistencia-leitura-replica`

**Status:** completed
**Modo:** Materialização direta (autonomo workflow content-only)

## Objetivo

Materializar 1 skill da Suíte DDIA Foundations v1.22 cobrindo problemas de replication lag (DDIA Ch 5) aplicados ao stack Supabase + Supavisor + Realtime:

- 3 problemas canônicos DDIA Ch 5 (read-after-write inconsistente, leituras não-monotônicas, prefixo causal violado)
- 3 soluções para Supabase (leitura no líder após escrita, sticky session por user_id, detecção stale via `pg_last_wal_replay_lsn()`)
- Tabela canônica Supavisor (porta 6543 transaction vs 5432 session/líder)
- Padrão "ler o próprio broadcast" (Realtime payload em vez de re-fetch que pode atingir réplica stale)

## Tarefas

| # | Tarefa | Status | Artefato |
|---|---|---|---|
| 1 | Criar diretório `kit/skills/consistencia-leitura-replica/` | ✅ | dir |
| 2 | Escrever `SKILL.md` com frontmatter PT-BR + 4 seções obrigatórias | ✅ | skill |
| 3 | Cross-ref ATIVO para `_shared-dados-distribuidos/glossary.md` (Phase 117) | ✅ | seção "Ver também" |
| 4 | Cross-ref ATIVO para `supabase-realtime/SKILL.md` (v1.8) | ✅ | seção "Realtime broadcast" |
| 5 | Cross-ref ATIVO para `multi-tenant-performance-scaling/SKILL.md` (v1.21) | ✅ | seção "Supavisor" |
| 6 | Code blocks SQL para `pg_last_wal_replay_lsn()` (líder + réplica wait) | ✅ | seção "Detecção stale" |
| 7 | Code block TS para "ler o próprio broadcast" | ✅ | seção "Realtime broadcast" |
| 8 | Tabela Supavisor (4 colunas: porta, modo, use case, connection string) | ✅ | seção "Supavisor routing" |

## REQs cobertos

- ✅ **LEITURA-01**: 3 problemas canônicos DDIA Ch 5 documentados em seções nomeadas com exemplo de bug real
- ✅ **LEITURA-02**: 3 soluções canônicas para Supabase (leitura no líder, sticky session, `pg_last_wal_replay_lsn()`)
- ✅ **LEITURA-03**: Supavisor read replica routing com tabela 4 colunas (porta/modo/use case/connection string)
- ✅ **LEITURA-04**: Realtime broadcast vs leitura DB com pitfall canônico + padrão "ler o próprio broadcast"

## Restrições aplicadas

- PT-BR completo no frontmatter `description:` + headings + narrativa.
- Termos técnicos canônicos preservados em EN dentro da descrição (`read-after-write`, `monotonic reads`, `consistent prefix reads`, `replication lag`, `pg_last_wal_replay_lsn`).
- Code blocks SQL/TS em EN com comentários PT-BR.
- Cross-refs ATIVOS — link Markdown relativo para outras skills, sem duplicar definições do glossário.
- Zero `src/core/` modificado (content-only).

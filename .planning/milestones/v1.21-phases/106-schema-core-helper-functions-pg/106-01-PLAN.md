# Phase 106 — Plan 01: Skills Schema + Performance

**Status:** completed
**Modo:** Materialização direta (autonomo workflow content-only)

## Objetivo

Materializar 2 skills canônicas que servem como base para toda a Suíte Multi-Tenant SaaS B2B v1.21:

1. `kit/skills/b2b-saas-architecture/SKILL.md` — schema canônico 7 tabelas + isolation strategy + JWT minimal
2. `kit/skills/multi-tenant-performance-scaling/SKILL.md` — Supavisor 6543 + STABLE helpers + partial indexes + partitioning + MVs

## Tarefas

| # | Tarefa | Status | Artefato |
|---|---|---|---|
| 1 | Criar diretório `kit/skills/b2b-saas-architecture/` | ✅ | dir |
| 2 | Criar diretório `kit/skills/multi-tenant-performance-scaling/` | ✅ | dir |
| 3 | Escrever skill `b2b-saas-architecture/SKILL.md` (~270 linhas) | ✅ | SKILL.md |
| 4 | Escrever skill `multi-tenant-performance-scaling/SKILL.md` (~250 linhas) | ✅ | SKILL.md |

## Conteúdo das skills

### b2b-saas-architecture
- 5 regras absolutas (Single Schema default, JWT minimal, slug imutável, super_admin via service_role, FKs CASCADE/RESTRICT explícitos)
- Tabela comparativa isolation strategies
- DDL completo das 7 tabelas + slug_history
- Custom Access Token Hook (SQL + config.toml)
- Set super_admin via service_role (TypeScript)
- Trigger track_org_slug_change
- 5 anti-patterns

### multi-tenant-performance-scaling
- 5 regras absolutas (Supavisor 6543, STABLE helpers, partial indexes, partitioning threshold, MV refresh CONCURRENTLY)
- Connection string Vercel + Prisma `connection_limit=1`
- Helper function STABLE com diff de performance 200×
- 6 partial indexes obrigatórios (organization_members, roles, permissions, role_permissions, departments, audit_logs)
- LIST partitioning por org_id (audit_logs example)
- MV per-tenant + pg_cron refresh schedule
- Queries de diagnóstico (pg_stat_statements, sem-index detection)
- 5 anti-patterns

## REQs cobertos

- **ARCH-01**: Single Schema + org_id + RLS é estratégia canônica documentada com tabela comparativa ✅
- **ARCH-02**: Schema canônico 7 tabelas com FKs explícitas ✅
- **ARCH-05**: JWT claims minimal (super_admin: bool em app_metadata apenas) com anti-pattern explícito ✅
- **ARCH-06**: Supavisor transaction mode 6543 + partitioning + MVs per-tenant ✅

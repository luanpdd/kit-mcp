# Phase 107 — Plan 01: Skill + Agent

**Status:** completed
**Modo:** Materialização direta

## Tarefas

| # | Tarefa | Status |
|---|---|---|
| 1 | Escrever `kit/skills/org-onboarding-flow/SKILL.md` (~210L) | ✅ |
| 2 | Escrever `kit/agents/org-onboarding-implementer.md` (~180L) | ✅ |

## Conteúdo

**Skill:** 5 regras absolutas (atomicidade, primeiro admin = creator, slug imutável, wizard async, slug uniqueness) + RPC `create_organization` SQL completa + Edge Function setup wizard + state machine + 4 anti-patterns.

**Agent:** delegation pattern para `supabase-migration-writer` + `supabase-edge-fn-writer`. AskUserQuestion para slug strategy + wizard features. Validação de Phase 106 pré-requisita. Output integrado.

## REQs cobertos (3/3)

- ✅ ORG-01: Skill documenta fluxo signup → criar org → primeiro admin → setup wizard
- ✅ ORG-02: Agent gera código (migration via supabase-migration-writer + Edge Function via supabase-edge-fn-writer)
- ✅ ORG-03: Skill cobre slug imutável como default + alternativa redirect trail explicitamente

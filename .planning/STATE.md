---
state_version: 1.0
milestone: v1.10
milestone_name: — SRE Engagement
status: in_progress
last_updated: "2026-05-07T07:51:44Z"
progress:
  total_phases: 6
  completed_phases: 5
  total_plans: 30
  completed_plans: 29
---

# STATE.md — sessão atual

> Atualizado por workflows e mantido entre sessões. Source of truth para "onde paramos".

## Posição Atual

Fase: 41 — Gates QA + README + CHANGELOG — EM ANDAMENTO
Plano: 04 (README v1.10 SRE Engagement section) — CONCLUÍDO (Onda 2 da Phase 41)

**Plan 41-04 concluído** (commit `4d25c90` em `README.md` 55 insertions / 0 deletions — pure additive patch): nova seção `### SRE Engagement suite (v1.10)` inserida entre `### Observability suite (v1.9)` (existente, linha 66) e separador `---` (existente, agora linha 159) preservando ordem cronológica das suítes (v1.8 Supabase → v1.9 Observability → v1.10 SRE). Conteúdo canônico replicado verbatim do PLAN.md sem rewrites — single source of truth preservada. Citação ao livro fonte presente: *Site Reliability Engineering: How Google Runs Production Systems* (Beyer, Jones, Petoff, Murphy — Google/O'Reilly, 2016) — paridade com seção v1.9 que cita Charity Majors et al. *Observability Engineering*. Inventário completo: **6 skills** (`_shared-sre/glossary.md` + `sre-risk-management` + `four-golden-signals` + `eliminating-toil` + `blameless-postmortems` + `production-readiness-review`) + **4 agents** (`golden-signals-instrumenter` + `toil-auditor` + `postmortem-writer` + `prr-conductor`) + **6 commands** (`/sre <subcommand>` orchestrator + `/golden-signals` + `/auditar-toil` + `/postmortem` + `/prr` + `/risk-budget`) + **3 audit gates** (`golden-signals-coverage` blocking pre-verify + `postmortem-template-required` blocking pre-conclude + `prr-checklist-coverage` blocking pre-verify). Bloco "Lifecycle integration" lista 3 cross-refs Phase 40: `/forense` (INT-FW-V2-01) + `/concluir-marco` opt-in PRR gate (INT-FW-V2-02) + `/auditar-marco` auto-`/auditar-toil` (INT-FW-V2-03). Quick start example end-to-end demonstra workflow canônico SRE: `/sre prr --feature "checkout v2"` → `/sre golden-signals supabase/functions/orders/index.ts` → `/sre toil` → `/forense "checkout SLO burn rate"` → `/sre postmortem --from-investigation` → `/sre risk-budget`. Composição production engineering stack explicitada: v1.8 (Supabase) + v1.9 (Observability) + v1.10 (SRE). Smoke validation ALL_PASS (13 categorias): SRE section count=1, v1.9 section preserved=1, ordering correct (v1.9 line 66 → v1.10 line 104), book citation count=1 each (Site Reliability Engineering + Beyer/Jones/Petoff/Murphy + Google/O'Reilly 2016), inventory headings (6 skills=1 + 4 agents=1 + 6 commands=2 [v1.9+v1.10] + 3 audit gates=1), 6 skills v1.10 cada ≥1×, 4 agents v1.10 cada ≥1×, 6 commands v1.10 cada ≥1×, 3 gates v1.10 cada=1 (string única), 3 INT-FW-V2-* refs cada=1, Quick start end-to-end (heading + 4 commands canônicos cada=1), badges (linhas 3-6) byte-identical preserved (head -6 confirmado: title + npm version + npm downloads + CI + License MIT), `git diff --numstat README.md` = `55 0 README.md` (zero deletions, pure additive). SUMMARY.md gravado em `.planning/phases/41-gates-qa-readme-changelog/41-04-readme-v110-section-SUMMARY.md`. Cobre QA-SRE-04 integralmente. **Phase 41 — Plans 01 + 02 + 03 + 04 concluídos (4/5 plans Phase 41)** — apenas Plan 41-05 (CHANGELOG v1.10 entry) pendente para fechar Phase 41 e completar Onda 3 do milestone v1.10. Ready para execução de 41-05 → `/concluir-marco v1.10` → `/publicar`.

**Plan 41-01 anteriormente concluído** (paralelo com 41-02 + 41-03) — `gates/golden-signals-coverage.md` (NEW, 133 linhas) criado como gate bash 3.2-portable blocking pre-verify validando que código de serviço/Edge Function tocado em fase contém os 4 golden signals (cap 6 livro Google SRE — *Monitoring Distributed Systems*): Latency=histogram + Traffic=counter + Errors=counter + Saturation=gauge. Regex inclusiva (`histogram|Histogram` / `counter|Counter|createCounter` / `gauge|Gauge|saturation|Saturation`) — vendor-neutral (OTel, Prometheus, StatsD, Borgmon-like). Descobre código em `supabase/functions/**`, `src/**`, `lib/**` (.ts/.js/.mjs/.py). Skip gracefully (INFO + exit 0) se nenhum dir de código existe ou está vazio — projetos content-only não falham. FAIL exit 1 lista signals ausentes + sugere `/sre golden-signals <service>` ou `/golden-signals` para gerar instrumentação OTel canônica + cross-refs ATIVOS skill `kit/skills/four-golden-signals/SKILL.md` (Phase 36 / SKFD-SRE-02) + agent `kit/agents/golden-signals-instrumenter.md` (Phase 37 / AGCORE-SRE-01). Frontmatter canônico (4 campos: `id: golden-signals-coverage`, `stage: pre-verify`, `blocking: true`, `description` 191 chars ≤ 200). Smoke validation ALL_PASS (3 fixtures sintéticos): (1) sem signals → FAIL exit 1 com `Latency(histogram) Traffic(counter) Errors(counter) Saturation(gauge)` lista; (2) com 4 signals OTel canônicos → PASS exit 0 com counts; (3) content-only (só markdown) → INFO skip + exit 0. Bash 3.2-portable validado — sem `mapfile`/`readarray`/`[[ =~ ]]` captures/`declare -A`/`coproc`/`${var,,}`. Discoverable via `node bin/cli.js gates list` em ordem alfabética entre `dependency-check` e `no-personal-uuid`. Toggle warn-only documentado como deferido (`workflow.golden_signals_coverage_warn` citado mas gate atual não consulta config). Commit `23bbac5` puro additive (133 insertions / 0 deletions). SUMMARY.md gravado em `.planning/phases/41-gates-qa-readme-changelog/41-01-gate-golden-signals-coverage-SUMMARY.md`. Cobre QA-SRE-01 integralmente. Observação documentada: kit-mcp atual tem `src/` com 24 .ts/.js/.mjs (CLI source) — gate FAILs corretamente conforme spec; acceptance criteria T3 do plano expectava `passed (skip)` baseado em hipótese desalinhada. Comportamento do gate é correto; expectativa do plano sobre estrutura kit-mcp foi otimista.

**Plan 41-02 também concluído** (paralelo com 41-01 + 41-03; commit `4e481a6` em `gates/postmortem-template-required.md` 127 insertions / 0 deletions — NEW file): gate bash 3.2-portable **blocking pre-conclude** que enforca "no postmortem left unreviewed" (cap 15 livro Google SRE — *Postmortem Culture: Learning from Failure*). Em `/concluir-marco`, bloqueia se há investigação registrada em `.planning/investigations/` sem postmortem correspondente em `.planning/postmortems/`. Frontmatter canônico (4 campos: `id: postmortem-template-required`, `stage: pre-conclude`, `blocking: true`, `description` 174 chars ≤ 200 limite). Bloco `## Check` com bash 3.2-portable (`#!/usr/bin/env bash` + `set -e`, sem `mapfile`/`readarray`/`[[ =~ ]]`/`declare -A`). Detecta 2 patterns de storage: pattern A `<id>.md` single-file via `find -maxdepth 1 -type f -name "*.md"` + pattern B `<id>/STATE.md` subdir via `find -mindepth 2 -maxdepth 2 -type f -name "STATE.md"`. ID extraction por `${base%.md}` (pattern A) OU `basename "$(dirname "$inv_path")"` (pattern B). Match por basename `<id>` — coopera com handoff `postmortem-writer --from-investigation <id>` que produz `.planning/postmortems/<id>.md` exato. Detecção `Status: INCONCLUSIVE` como exceção via `grep -qiE "^Status:.*INCONCLUSIVE|^.*Status.*INCONCLUSIVE"` (sem root cause = sem postmortem exigido — alinhado com Plan 40-01 lista canônica de "Quando NÃO sugerir chain `/postmortem`"). Skip gracefully se `.planning/investigations/` ausente OU vazio (INFO + exit 0). Output formato canônico para gate-runner: primeira linha `PASS:`/`FAIL:`/`INFO:`. Mensagem FAIL inclui contagem de pares ausentes, sugestão acionável `/postmortem --from-investigation <id>`, cross-ref ao skill `kit/skills/blameless-postmortems/SKILL.md` + agent `kit/agents/postmortem-writer.md`, princípio cap 15 citado literalmente "No postmortem left unreviewed". Bloco `## Why` com cross-refs ATIVOS Markdown literais: `[postmortem-writer](../kit/agents/postmortem-writer.md)` (Phase 37 / AGCORE-SRE-03) + `[blameless-postmortems](../kit/skills/blameless-postmortems/SKILL.md)` (Phase 36 / SKFD-SRE-04) + comando `/postmortem --from-investigation <id>` (Phase 38 / CMD-SRE-03) + chain documentado em `kit/commands/forense.md` bloco `<sre_integration>` (Plan 40-01 / INT-FW-V2-01). Bloco `## Configuração` documenta toggle `workflow.postmortem_required_warn` (deferido — gate atual lê apenas presença/ausência de pares investigation↔postmortem, não consulta config). Smoke validation ALL_PASS (4 cenários): (1) `head -8` mostra frontmatter shape correto; (2) description length = 174 chars ≤ 200; (3) `node bin/cli.js gates list` discovers gate; (4) gate executado em kit-mcp atual = `verdict=passed exitCode=0 stdout="INFO: .planning/investigations não existe — projeto sem incidents registrados. Gate skipped."` (codebase sem incidents); fixture incident sem postmortem = FAIL exit 1 com mensagem "1 investigação(ões) sem postmortem em .planning/postmortems/: incident-2026-05-07-001" + sugestão `/postmortem --from-investigation`; fixture com par investigation+postmortem = PASS exit 0; fixture INCONCLUSIVE = PASS exit 0 com mensagem "INFO: investigation 'incident-2026-05-07-002' marcada INCONCLUSIVE — sem root cause, postmortem não exigido". Decisão **blocking por default** (≠ `omm-no-regression` que é warn-only) justificada — cultura blameless é não-negociável uma vez instituída (cap 15). Loop fechado SRE v1.9 → v1.10 enforced: `/forense` (v1.9) → `incident-investigator` produz `.planning/investigations/<id>.md` → `/postmortem --from-investigation <id>` (v1.10) produz `.planning/postmortems/<id>.md` → gate `postmortem-template-required` enforça correspondência antes de `/concluir-marco` arquivar milestone. Pre-conclude gate stack agora tem 2 gates: `omm-no-regression` (v1.9 — warn-only) + `postmortem-template-required` (v1.10 — blocking). SUMMARY.md gravado em `.planning/phases/41-gates-qa-readme-changelog/41-02-gate-postmortem-template-required-SUMMARY.md`. Cobre QA-SRE-02 integralmente.

**Plan 41-03 também concluído** (paralelo com 41-01 + 41-02)
Detalhes Plan 41-03: `gates/prr-checklist-coverage.md` (NEW, 128 linhas) criado como gate bash 3.2-portable blocking pre-verify validando que cada `.planning/prr/**/*.md` cobre os 6 axes canônicos do PRR (cap 32 livro Google SRE — *Evolving SRE Engagement Model*): System Architecture / Instrumentation / Emergency Response / Capacity Planning / Change Management / Performance. Match case-insensitive via `grep -qiE` aceita variantes naming (Architecture sem "System", Monitoring sem "Instrumentation", Capacity sem "Planning"); filtro `grep -E "^## "` antes do match limita validação a headings H2 (minimiza false-positive em prosa). Skip gracefully se `.planning/prr/` ausente OR vazio (INFO + exit 0). Mensagem FAIL lista axes ausentes por arquivo (debugabilidade). Cross-refs ATIVOS Markdown literais: `kit/skills/production-readiness-review/SKILL.md` (Phase 36 / SKFD-SRE-05) + `kit/agents/prr-conductor.md` (Phase 37 / AGCORE-SRE-04) + `/prr` command (Phase 38 / CMD-SRE-04) + `gates/concluir-marco.md` PRR gate opcional (Phase 40 / INT-FW-V2-02). Princípio canônico citado: "Pular um axe = aprovação inválida (lacuna oculta vira incident em 6 meses)" — regra absoluta da skill production-readiness-review. Frontmatter canônico (4 campos: `id: prr-checklist-coverage`, `stage: pre-verify`, `blocking: true`, `description` 178 chars ≤ 200 limite). Smoke validation ALL_PASS (5 cenários): (1) PASS fixture com `## Axe N: <nome>` prefixo → exit 0; (2) FAIL fixture com 3 axes ausentes → exit 1 com lista `Axe3(EmergencyResponse) Axe4(CapacityPlanning) Axe5(ChangeManagement)`; (3) Naming variants `## Architecture` / `## Monitoring` / `## Capacity` sem prefixo Axe → exit 0; (4) Diretório vazio → INFO skip + exit 0; (5) Diretório ausente (kit-mcp atual) → INFO skip + exit 0. Bash 3.2-portable validado — sem `mapfile`/`readarray`/`[[ =~ ]]`/`declare -A`. Toggle warn-only documentado como deferido (`workflow.prr_checklist_coverage_warn` citado mas gate atual não consulta config). Commit `bf7bde7` puro additive (128 insertions / 0 deletions). SUMMARY.md gravado em `.planning/phases/41-gates-qa-readme-changelog/41-03-gate-prr-checklist-coverage-SUMMARY.md`. Cobre QA-SRE-03 integralmente. Plans 41-01 (golden-signals-coverage) + 41-02 (postmortem-template-required) executando em paralelo no momento (untracked files visíveis em git status). Phase 41 ainda tem plans 41-04 (README v1.10 section) + 41-05 (CHANGELOG v1.10 entry) pendentes.

**Plan 40-03 anteriormente concluído** — bloco `<sre_integration>` adicionado a `kit/commands/auditar-marco.md` auto-invoca `/auditar-toil` quando `workflow.audit_milestone_toil=true` (default). Loop fechado SRE: `/auditar-marco` → `/auditar-toil` → `/auditar-observabilidade` → `omm-auditor` consulta `.planning/TOIL-AUDIT.md` para score Capacidade 3.
Última atividade: 2026-05-07 — Plan 41-04 concluído (commit `4d25c90` em `README.md` 55 insertions / 0 deletions — pure additive; nova seção `### SRE Engagement suite (v1.10)` entre v1.9 e separador `---`; 6 skills + 4 agents + 6 commands + 3 audit gates listados; book citation Beyer/Jones/Petoff/Murphy 2016 incluída; lifecycle integration + quick start end-to-end; cobre QA-SRE-04). Plan 41-03 anteriormente concluído (commit `bf7bde7` em `gates/prr-checklist-coverage.md` 128 insertions / 0 deletions — NEW file). Plan 40-03 anteriormente concluído (commit `19eb4dd` em `kit/commands/auditar-marco.md` +81/-1 linhas — puro additive; "deleção" é apenas o marker "no newline at end of file"); frontmatter v1.7+ byte-idêntico preservado (anti-pitfall A2 — `name: auditar-marco`, `description: Audita a conclusão do milestone contra a intenção original antes de arquivar`, `argument-hint: "[version]"`, `allowed-tools: Read/Glob/Grep/Bash/Task/Write` com 6 entries incluindo `Task`); bloco v1.9 `<observability_integration>` (INT-FW-04 + `audit_milestone_omm` + `framework:auditar-observabilidade`) preservado byte-a-byte; novo bloco `<sre_integration>` posicionado **após** `</observability_integration>` como última seção do arquivo; conteúdo do bloco: heading "Toil scoring auto-invocação (v1.10 — INT-FW-V2-03)", flag `workflow.audit_milestone_toil=true` mencionado 2× (descrição + default), chamada literal `Skill(skill="framework:auditar-toil")` paridade com `framework:auditar-observabilidade` v1.9, parágrafo introdutório do `/auditar-toil` (analisa git log ≤ 90d + scripts shell + runbooks + .planning/phases/SUMMARY.md; classifica P0/P1/P2 esforço × frequência; produz `.planning/TOIL-AUDIT.md`; cap 5 livro Google SRE *Eliminating Toil* — manual + repetitivo + automatizável + tático + sem valor durável + escala linear), bloco "Loop fechado canônico" 4 etapas (`auditar-marco` → `auditar-toil` → `auditar-observabilidade` → `omm-auditor` consulta TOIL-AUDIT.md → OMM-REPORT.md inclui Cap 3 → MILESTONE-AUDIT.md anexos), bloco "Por que rodar /auditar-toil ANTES" com regra absoluta `omm-auditor` "score Cap 3 > 3 exige TOIL-AUDIT.md fresco ≤ 30d com % toil < 30%" (Phase 39 INT-OBS-02), tabela 5-row score Capacidade 3 (`< 15%` → 5 / `15-30%` → 4 / `30-50%` → 3 / `50-60%` → 2 / `> 60%` → 1) replicada em paridade com `omm-auditor.md` Step 1 (single source of truth distribuída), output esperado de `TOIL-AUDIT.md` documentado (4 itens — % toil pelo time, P0/P1/P2 com ROI = freq × tempo / esforço, sugestões pg_cron/PostToolUse/kit-mcp/GHA, anti-toil-by-design `/discutir-fase`), skill consultada `[eliminating-toil](../skills/eliminating-toil/SKILL.md)` cross-ref ATIVO, bloco "Quando desligar gate" com 3 contraindicações (solo dev / ≤ 30d / repo bibliotecário sem ops — kit-mcp content-only sem deploy citado), 4 anti-patterns prevenidos (skip "está OK há tempo" / TOIL-AUDIT.md gerado mas ignorado / toil = features pequenas / toil ≠ overhead), rodapé `**REQ:** INT-FW-V2-03`; smoke validation ALL_PASS — `head -12` frontmatter byte-idêntico confirmado, `<sre_integration>` count=1 + `</sre_integration>` count=1, `<observability_integration>`/`</observability_integration>` count=1 cada (preservado), INT-FW-04 count=4 (preservado), `audit_milestone_omm` count=1 (preservado), `framework:auditar-observabilidade` count=1 (preservado), `workflow.audit_milestone_toil` count=2, `framework:auditar-toil` count=1, cross-refs Markdown ativos `[eliminating-toil](../skills/eliminating-toil/SKILL.md)` count=1 + `[toil-auditor](../agents/toil-auditor.md)` count=1 + `[omm-auditor](../agents/omm-auditor.md)` count=1, conteúdo canônico cap 5 (`cap 5` count=3, `Eliminating Toil` count=2, `≤ 50%|< 50%` count=3), score table 5-row Cap 3 (`| < 15% |` / `| 15-30% |` / `| 30-50% |` / `| 50-60% |` / `| > 60% |` cada count=1), `Loop fechado canônico` count=1, `TOIL-AUDIT.md` count=8, regra fresh count=2 (`≤ 30 dias|≤ 30d|fresco ≤ 30`), `Quando desligar gate` count=1, `INT-FW-V2-03` count=3 (heading + cap fim de loop + rodapé REQ), diff numstat 81/1 puro additive; smoke sync (`node bin/cli.js sync install claude-code --mode copy`) propaga bloco para `.claude/commands/auditar-marco.md` (`<sre_integration>`=1, `audit_milestone_toil`=2, `framework:auditar-toil`=1, `INT-FW-V2-03`=3); cobre INT-FW-V2-03 integralmente. SUMMARY.md gravado em `.planning/phases/40-patches-em-fluxo-framework/40-03-auditar-marco-toil-cross-ref-SUMMARY.md`. **Phase 40 — Plans 01 + 02 + 03 concluídos (3/3 plans Phase 40)** — Onda 2 do milestone v1.10 100% fechada (Phases 39 + 40); ready para Phase 41 (gates + docs).

**Plan 40-02 também concluído** (commit `e10afd3` em `kit/commands/concluir-marco.md` +55/-1 linhas — patch editorial puro additive; o "1 deletion" é apenas o `\ No newline at end of file` flag git de extensão da última linha, conteúdo é puro additive): bloco `<sre_integration>` adicionado como última seção do command imediatamente após `</observability_integration>`; frontmatter v1.7+ byte-idêntico preservado (anti-pitfall A2 — `type: prompt`, `name: concluir-marco`, `description: Arquiva milestone concluído e prepara para próxima versão`, `argument-hint: "<version>"`, `allowed-tools: [Read, Write, Bash]` inalterados); bloco `<observability_integration>` v1.9 (INT-FW-05 OMM regression gate) preservado byte-a-byte; novo bloco contém: heading "PRR gate opcional para features production-bound (v1.10 — INT-FW-V2-02)"; flag `workflow.complete_milestone_prr_gate` mencionado 2× com default `false` (≠ `complete_milestone_omm_gate` v1.9 que é `true` — opt-in até maturidade SRE); 4 passos do gate (listar production-bound features → procurar PRR-REPORT.md → verificar status → arquivar/bloquear); status table 3-row (`passed` 6/6 axes ≥3/5 = arquivável / `passed-with-warnings` P1 pendente = arquivável com warnings / `failed` P0 reprovado = BLOQUEIA); bloco "Quando ligar gate" com 4 condições (paid feature, SLO em `.planning/slos/`, on-call rotation, postmortem culture em `.planning/postmortems/`) + regra "≥ 2 dos 4 = liga gate"; bloco "Quando manter gate desligado" com 4 contraindicações (early stage, solo, POC, sem cultura remediation); cross-refs ATIVOS Markdown literais `[production-readiness-review](../skills/production-readiness-review/SKILL.md)` count=1 + `[prr-conductor](../agents/prr-conductor.md)` count=2 (1 no bloco passos + 1 em anti-patterns); comando literal `/prr --feature` + `/sre prr --feature` mencionado; gate file `gates/prr-checklist-coverage.md` (Phase 41 — QA-SRE-03) referenciado para fechar loop; 4 anti-patterns prevenidos (production-bound sem PRR, status failed aceito, auto-PRR pelo time dev, gate ligado em early stage); "cap 32" e "Evolving SRE Engagement Model" referenciados literalmente; rodapé `**REQ:** INT-FW-V2-02` presente; smoke ALL_PASS — `<sre_integration>` count=1, `</sre_integration>` count=1, `<observability_integration>` count=1 preservado, `</observability_integration>` count=1 preservado, INT-FW-05 count=2 preservado, `complete_milestone_omm_gate` count=2 preservado, `workflow.complete_milestone_prr_gate` count=2, `[production-readiness-review]` skill cross-ref count=1, `[prr-conductor]` agent cross-ref count=2, `production-bound` count=6, `cap 32` count=3, `Evolving SRE Engagement Model` count=1, `/prr --feature` count=1, status table 3-row presente (`passed`, `passed-with-warnings`, `failed` cada count=1), "Quando ligar gate" 1× + "2 dos 4" 1×, `prr-checklist-coverage` count=1, `INT-FW-V2-02` count=2; frontmatter byte-idêntico via `head -10`; `node bin/cli.js sync install claude-code --mode copy` propaga bloco para `.claude/commands/concluir-marco.md` (gitignored mas geração reprodutível); cobre INT-FW-V2-02 integralmente. SUMMARY.md gravado em `.planning/phases/40-patches-em-fluxo-framework/40-02-concluir-marco-prr-gate-SUMMARY.md`. Gates v1.9 (OMM regression) e v1.10 (PRR coverage) coexistem ortogonalmente — OMM mede observability maturity, PRR mede production readiness; v1.9 default `true` (estabelecido) vs v1.10 default `false` (opt-in até maturidade) é design intencional.

**Plan 40-01 também concluído** (commit `7ebd3f1` em `kit/commands/forense.md` +56/-1 linhas — patch puramente aditivo; "1 deletion" é apenas o `\ No newline at end of file` marker, conteúdo é zero linhas removidas/modificadas): bloco `<sre_integration>` adicionado como **última seção** após `</observability_integration>` v1.9 (INT-FW-06 Core Analysis Loop preservado byte-a-byte); frontmatter v1.9 byte-idêntico (anti-pitfall A2 — `type: prompt`, `name: forense`, `description: Investigação post-mortem de workflows framework com falha — analisa histórico git, artefatos e estado para diagnosticar o que deu errado`, `argument-hint: "[descrição do problema]"`, `allowed-tools: Read, Write, Bash, Grep, Glob` inalterados); bloco contém: heading "Chain `/postmortem` após Core Analysis Loop (v1.10 — INT-FW-V2-01)"; distinção fundamental Forense (diagnóstico evidence-based read-only — "o que aconteceu? onde está a evidência?" — output `.planning/forensics/report-<ts>.md` para investigador) vs Postmortem (aprendizado organizacional blameless — "o que aprendemos? o que mudaremos?" — output `.planning/postmortems/<id>.md` para organização inteira / "no postmortem left unreviewed") via tabela 2-row Etapa/Pergunta/Output/Audiência; introdução conceitual Forense **diagnostica** vs postmortem **transforma diagnóstico em aprendizado durável** (anti-pattern hero culture "fixei o bug, vamos seguir"); 3 condições de trigger sugerido não-bloqueante (≥1 hipótese VALIDATED apontando root cause acionável OU incident impactou usuário não apenas dev experience OU workflow framework crashou em produção não dogfooding); template de sugestão exibido ao final do relatório forense com comando literal `/postmortem --from-investigation <forensic-id>` + cross-ref canônico Markdown skill+agent; chain canônico de fluxo (Falha detectada → /forense "<descrição>" diagnóstico evidence-based → Core Analysis Loop fecha VALIDATED → /postmortem --from-investigation <id> blameless write-up → Action Items P0/P1 viram tarefas em milestone atual ou próximo → Wheel of Misfortune treina novos engineers cap 15); 3 exceções explícitas de não-trigger (forense INCONCLUSIVE em todas as hipóteses → sugerir nova investigação ao invés / falha trivial documentada `.gitignore` typo sem impacto a usuário / investigação cancelada pelo user antes do Core Analysis Loop fechar); cross-refs ATIVOS Markdown literais `[blameless-postmortems](../skills/blameless-postmortems/SKILL.md)` skill (knowledge canônico cap 15) + `[postmortem-writer](../agents/postmortem-writer.md)` agent (writer que executa); cultura blameless é não-negociável explicitada (postmortem foca em **sistema** — controles ausentes / signals não monitorados / escalation paths frágeis — NUNCA em **pessoas** — "dev X esqueceu de testar"); referência cap 15 livro Google SRE *Postmortem Culture: Learning from Failure* + frase canônica "No postmortem left unreviewed" + anti-pattern blame culture explicitamente prevenido pelo template do `postmortem-writer`; `/postmortem --from-investigation` mencionado 3× (≥2 target — template de sugestão + fluxo canônico ASCII + chain de fluxo); `INT-FW-V2-01` rodapé presente 2×; padrão tag XML-like `<sre_integration>...</sre_integration>` consistente com `<observability_integration>` v1.9 no mesmo arquivo; smoke validation ALL_PASS — `head -12` frontmatter byte-idêntico, `<sre_integration>` count=1, `</sre_integration>` count=1, `<observability_integration>` count=1 preservado, `</observability_integration>` count=1 preservado, `INT-FW-06` count=1 preservado, `[blameless-postmortems](../skills/blameless-postmortems/SKILL.md)` count=1, `[postmortem-writer](../agents/postmortem-writer.md)` count=1, `/postmortem --from-investigation` count=3, `cap 15` count=1, `Postmortem Culture` count=1, `blameless` count=5 (≥2 target), `No postmortem left unreviewed` count=1, `INT-FW-V2-01` count=2, diff numstat 56 insertions/1 deletion (deletion = no-newline-at-EOF marker apenas), `node bin/cli.js sync install claude-code --mode copy` propaga bloco para `.claude/commands/forense.md` (`<sre_integration>` count=1, `INT-FW-V2-01` count=2, `/postmortem --from-investigation` count=2); workflow `.claude/framework/workflows/forensics.md` inalterado (patch é editorial no command em `kit/commands/`, não toca lógica de execução do workflow); v1.9 (Core Analysis Loop / `<observability_integration>` cobre **como diagnosticar** método científico) e v1.10 (`<sre_integration>` chain `/postmortem` cobre **o que fazer com o diagnóstico** postmortem blameless) coexistem; cobre INT-FW-V2-01 integralmente. SUMMARY.md gravado em `.planning/phases/40-patches-em-fluxo-framework/40-01-forense-postmortem-chain-SUMMARY.md`. Plans 40-01 + 40-02 + 40-03 concluídos em paralelo fecham Phase 40 (3/3) — Onda 2 v1.10 100% fechada (Phases 39 + 40); ready para Phase 41 (gates + docs).

**Plan 39-06 concluído anteriormente** (commit `f92d95f` em `kit/agents/supabase-storage-implementer.md` +156/-0 linhas — patch puramente aditivo; nova seção `## Saturation signal — bucket size + quota` posicionada **entre** `## Observabilidade integrada` e `## Ver também` v1.8; frontmatter v1.8 preservado byte-a-byte (anti-pitfall A2 — `name: supabase-storage-implementer`, `tools: Read, Write, Edit, Bash, Grep, Glob, mcp__supabase__execute_sql`, `color: orange` byte-idêntico); seção contém: bloco cross-ref Markdown literal para `[four-golden-signals](../skills/four-golden-signals/SKILL.md)` (cap 6 SRE) + `[golden-signals-instrumenter](./golden-signals-instrumenter.md)` agent; introdução conceitual identificando quota plan como recurso mais escasso explícito (anti-pattern white-box monitoring sem detecção precoce); tabela "Saturation = bucket size ÷ quota plan" 4 plans (Free 1 GB / Pro 100 GB / Team 1 TB / Enterprise custom) com thresholds 80% (yellow) + 95% (red); Signal 1 — Gauge: 2 `createObservableGauge` (`storage_bucket_bytes` em bytes + `storage_saturation_pct` ratio 0..1 com `Deno.env.get('SUPABASE_PLAN_QUOTA_BYTES')`); SQL helper canônico `public.storage_bucket_sizes_bytes()` com `security definer` + `set search_path = ''`; Signal 2 — Counter: `createCounter('storage_quota_warnings_total')` em pre-check de upload (threshold 80pct/95pct); Cron schedule com materialized view `obs.storage_saturation` + `cron.schedule('refresh_storage_saturation', '* * * * *', ...)`; Alert SLO YAML `storage_quota_healthy` event-based (target 0.99, window 30d_sliding, good_event saturation_pct < 0.80); Output do agent listando 5 artefatos (function, MV, gauge, counter, SLO); 4 anti-patterns (saturation ≠ % disco; threshold direto vs SLO event-based; polling per-request vs MV+cron; quota hardcoded vs env var); `## Ver também` ganhou 2 entries (sem reordenar 5 existentes); smoke validation ALL_PASS — frontmatter byte-idêntico (head -6 confirmou), `## Saturation signal` heading count=1, `[four-golden-signals]` cross-ref count=2, `[golden-signals-instrumenter]` cross-ref count=2, `createObservableGauge` count=2, `createCounter` count=1, métricas canônicas combined count=11 (bucket_bytes + saturation_pct + quota_warnings_total + sizes_bytes + quota_healthy), `security definer` count=1, `## Observabilidade integrada` preservado count=1, `## Ver também` preservado count=1, diff numstat 156/0 (puro additive); cobre INT-SB-V2-04 integralmente. **Phase 39 — Plan 06 de 6 concluído** (Onda 2 v1.10 — 5 dos 6 plans 39-* já concluídos via parallel executors; este é o último).

**Plan 39-04 também concluído** (commit `d3eec5d` em `kit/agents/supabase-architect.md` +49/-0): nova seção `## Production Readiness Review` adicionada após `## Observabilidade integrada` com tabela 6 axes adaptada ao contexto Supabase (System Architecture: single project = SPOF mitigado por branches Pro; Instrumentation: 4 golden signals em Edge Functions + obs.events + audit hooks + SLI/SLO; Emergency Response: runbook RLS broken + on-call + postmortem template; Capacity Planning: Spend Cap + branch billing fora do cap + pgvector index size + Edge concurrent; Change Management: declarative + reverso + RLS git-versioned + supabase functions deploy idempotente; Performance: load test + p99 baseline + RLS explain plan sem seq scan + index coverage); 3 engagement models (Simple PRR internal/dogfood, Early Engagement default Edge user-facing, Frameworks/SRE Platform); blocos "Quando re-rodar PRR" + frase "PRR NÃO é one-shot" + 4 anti-patterns (auto-PRR, deploy-primeiro, pular axe, "acreditamos"); template de output extendido com `## 9. Observabilidade` placeholder dinâmico + `## 10. PRR pré-production` (5 bullets — invocar /sre prr ou /prr + 6 axes + 3 models + P0 blocker P1 scheduled + reviewer ≠ time dev) entre `## 8. Próximos passos` e fim do bloco markdown; cross-refs ATIVOS Markdown literais `[production-readiness-review](../skills/production-readiness-review/SKILL.md)` count=1 + `[prr-conductor](./prr-conductor.md)` count=2 (template + header); frontmatter v1.8 byte-preservado (anti-pitfall A2 — name, description, tools, color: blue inalterados); smoke ALL_PASS — heading count=1, ## Observabilidade integrada count=1 preservado, 6 axes ≥2× cada, 3 engagement models ≥2× cada, anti-patterns auto-PRR 2× + "PRR NÃO é one-shot" 1×, diff numstat 49/0 puro additive, sync install claude-code OK; cobre INT-SB-V2-02 integralmente. Phase 39 fechada com 6/6 plans.

**Plan 39-02 também concluído** (commits `e8acb63` / `0c00165` / `dbef143` / `923e64a` em `kit/agents/omm-auditor.md` +52/-0 linhas — patch editorial puro): frontmatter v1.9 byte-idêntico preservado (anti-pitfall A2 — `name: omm-auditor`, `description`, `tools: Read, Write, Bash, Grep, Glob, mcp__supabase__execute_sql`, `color: purple`); 3 patches em pontos canônicos: (1) Step 0 — bloco `**Capacidade 3 — Complexidade / Tech Debt (cross-ref [toil-auditor](./toil-auditor.md)):**` com shell snippet de check de `.planning/TOIL-AUDIT.md` + extração `% do tempo do time` via grep heuristic + regra de delegação Task quando audit ausente + flag stale > 30d; (2) Step 1 — tabela 5-row específica Cap 3 mapeando `% toil pelo time` → score 1-5 (`> 60%` → 1 / `50-60%` → 2 / `30-50%` → 3 / `15-30%` → 4 / `< 15%` → 5) + regra absoluta "score > 3 exige TOIL-AUDIT.md fresco ≤ 30d com `% toil < 30%`" + cross-ref preservado para skill `[observability-maturity-model](../skills/observability-maturity-model/SKILL.md)`; (3) Step 4 — exemplo `### Capacidade 3 — Complexidade / Tech Debt (3, ↑)` no template OMM-REPORT.md com sintoma literal `% toil pelo time = 38%` (abaixo da regra ≤ 50%), citação `.planning/TOIL-AUDIT.md` (path canônico), 4 itens P0 já automatizados, 6 itens P1 pendentes sem owner, 2 action items derivados marcados `[Cap 3]` (gate "anti-toil-by-design" no `/discutir-fase` P2 + designar owners P1); modelo 5-capacidade canônico preservado integralmente (resiliência, qualidade, complexidade, cadência, comportamento); commit `923e64a` aligns Step 1 heading short→long form para parity com smoke `grep -c "Capacidade 3 — Complexidade / Tech Debt" ≥ 3`; smoke validation ALL_PASS — `grep -c "[toil-auditor](./toil-auditor.md)"=1`, `grep -c "Capacidade 3 — Complexidade / Tech Debt"=3` (Step 0 / Step 1 / OMM-REPORT), `grep -c ".planning/TOIL-AUDIT.md"=4` (≥ 2× target), `grep -cE "≤ ?50%"=2`, score table 5-row preservada (`grep -c "^| 5 |"=1`), pure addition diff numstat 52/0, `kit sync install claude-code --mode copy` reproduz patches em `.claude/agents/omm-auditor.md` (5× toil-auditor / 3× Capacidade 3 / 11× TOIL-AUDIT); cobre INT-OBS-02 integralmente. Loop fechado: `omm-auditor` → `toil-auditor` → `eliminating-toil` skill via Markdown links — descoberta cross-agent natural sem hard-coupling. **Phase 39 fechada com 6/6 plans concluídos — Onda 2 do milestone v1.10 fechada (Phases 39 + 40 paralelas)**.

**Plan 39-03 também concluído** (commits `15f3091` T2 + `60f2281` T3 + `51723a8` T4 em `kit/agents/supabase-edge-fn-writer.md` +101 linhas líquidas — patch editorial substancial): frontmatter v1.8 byte-idêntico preservado (anti-pitfall A2 — `name: supabase-edge-fn-writer`, `description` 169 chars, `tools: Read, Write, Edit, Bash, Grep, Glob`, `color: cyan` inalterados); nova seção `## Four Golden Signals` inserida **entre** `## Observabilidade integrada` (v1.9) e `## Ver também` — v1.8/v1.9/v1.10 coexistem (Supabase Deno runtime + OTel SDK/spans/propagation + 4 instrumentos métricos canônicos cap 6 livro Google SRE); seção contém: cross-refs Markdown literais `[four-golden-signals](../skills/four-golden-signals/SKILL.md)` count=2 + `[golden-signals-instrumenter](./golden-signals-instrumenter.md)` count=2; tabela canônica 4 signals com 4 colunas (Signal/Instrumento/Dimensão/Valor padrão) e 4 rows (Latency=histogram bucketed exponencial [1,2,5,10,25,50,100,250,500,1000,2500,5000,10000,30000] ms com `result=success|error` separados / Traffic=counter por endpoint+http_method / Errors=counter por `error.type` enum 5-15 valores fechado — **NUNCA** `error.message` (cardinality explosion) / Saturation=ObservableGauge resource-specific identificado ANTES de instrumentar); snippet OTel TypeScript canônico copy-paste com 3 instrumentos (`createHistogram` count=2 + `createCounter` count=4 + `createObservableGauge` count=2) inicializados via `metrics.getMeter('<function_name>')`; wrapper `Deno.serve` instrumentado com `try/catch` capturando `latencyHistogram.record(performance.now() - start, { endpoint, result })` em ambos paths success/error com dimension `result` separada (`response.ok ? 'success' : 'error'`) + `errorsCounter.add(1, { endpoint, 'error.type': classifyError(err) })` em catch; função `classifyError(e: unknown): string` enum fechado com TimeoutError → `'timeout'` / ValidationError → `'validation'` / AuthError → `'auth'` / fallback `'unknown'`; tabela "Saturation por tipo de Edge Function" 4 rows (API simples GET/POST com leitura DB → `pg_pool` connections via `select count(*) from pg_stat_activity where state = 'active'` / RAG/embeddings → `concurrency_limit` provider externo via counter requests in-flight / Email/queue consumer cron→pgmq → `pgmq.queue_length` via `select msg_count from pgmq.metrics_<queue>` / Storage I/O heavy uploads → `egress_bandwidth` via bytes-out tracker em window); 4 anti-patterns prevenidos (Errors counter `err.message` cardinality explode → SEMPRE enum 5-15 / Latency mistura success+error → SEMPRE `result` dimension separa / Mean latency em vez de histogram → SEMPRE histogram com percentis backend-derived / Saturation genérico CPU% sem identificar recurso real → SEMPRE recurso scarcest da função); cross-refs ATIVOS append em `## Ver também` (7 entries pré-existentes preservadas + 2 novos: four-golden-signals skill v1.10/Phase 36 + golden-signals-instrumenter agent v1.10/Phase 37); smoke ALL_PASS — Latency=3 / Traffic=2 / Errors=3 / Saturation=6 (case-sensitive), frontmatter byte-idêntico via head -6, `## Four Golden Signals` count=1, `## Observabilidade integrada` count=1 preservado, `## Ver também` count=1 preservado, `node bin/cli.js sync install claude-code --mode copy` propaga "Four Golden Signals" para `.claude/agents/supabase-edge-fn-writer.md`; cobre INT-SB-V2-01 integralmente. SUMMARY.md gravado em `.planning/phases/39-patches-em-observabilidade-e-supabase/39-03-supabase-edge-fn-writer-golden-signals-SUMMARY.md`. **Phase 39 fechada com 6/6 plans concluídos — todos com SUMMARY.md gravados**.

## Milestone ativo

**v1.10 SRE Engagement** — incorporar técnicas do livro *Site Reliability Engineering* (Beyer, Jones, Petoff, Murphy — Google/O'Reilly, 2016) ao kit-mcp via skills/agentes/comandos novos com integração à Suíte Observabilidade v1.9 e Suíte Supabase v1.8.

**Estrutura em 3 ondas (Phases 36-41):**

- Onda 1 — Núcleo SRE (Phases 36-38): glossário + 5 skills foundationais + 4 agentes + 5 comandos + orquestrador `/sre`
- Onda 2 — Integração (Phases 39-40): patches Supabase (4 agentes) + patches fluxo framework (3 comandos) + patches observabilidade (2 artefatos)
- Onda 3 — Gates e docs (Phase 41): 3 audit gates + README + CHANGELOG

## Próximo passo

Phase 41 quase fechada — plans 41-01 + 41-02 + 41-03 + 41-04 concluídos (4/5). Apenas Plan 41-05 (CHANGELOG v1.10 entry) pendente. Após Phase 41 fechar:

1. `/executar-plano 41-05` — adicionar entry "v1.10.0 — SRE Engagement" ao `CHANGELOG.md`
2. `/concluir-marco v1.10` — arquivar milestone v1.10 SRE Engagement
3. `/publicar` — publicar release notes Notion + abrir PR no GitHub

## Bloqueadores

(nenhum)

## Todos pendentes

(vazio — planejamento concluído, execução virá em sessão seguinte)

## Histórico

- v1.0.0 → v1.5.3 — patches diversos
- v1.6.0 — concluído 2026-05-05 (16 audit REQs)
- v1.6.1 — concluído 2026-05-05 (kit doctor + upgrade-check)
- v1.7.0 — concluído 2026-05-06 (workflow compaction)
- v1.8.0 — concluído 2026-05-06 (Suíte Supabase: 11 skills + 7 agents + command + 5 gates)
- v1.8.1 — concluído 2026-05-06 (integração Supabase no fluxo)
- v1.9.0 — **publicada 2026-05-06** (Suíte Observabilidade: 11 skills + 5 agents + 6 commands + 3 gates + 11 patches; npm latest)
- **v1.10 — em planejamento** (SRE Engagement; ROADMAP criado 2026-05-06; aguardando execução)

## Contexto Acumulado

v1.10 estende a stack acumulada: v1.8 (Supabase) + v1.9 (Observabilidade) + v1.10 (SRE) formam suíte coesa de production engineering.

**Material-fonte v1.10:** *Site Reliability Engineering: How Google Runs Production Systems* (Beyer, Jones, Petoff, Murphy — Google/O'Reilly, 2016, ISBN 978-1-491-92912-4). Caps prioritários: 3 (Embracing Risk), 4 (SLOs), 5 (Eliminating Toil), 6 (Monitoring Distributed Systems / Four Golden Signals), 15 (Postmortem Culture), 32 (Evolving SRE Engagement Model / PRR).

**Como v1.10 conecta com v1.8 + v1.9:**

- `golden-signals-instrumenter` (v1.10) é especialização de `observability-instrumenter` (v1.9) — define os 4 sinais mínimos universais
- `postmortem-writer` (v1.10) é continuação natural de `incident-investigator` (v1.9) — após Core Analysis Loop fechar, postmortem documenta blameless
- `prr-conductor` (v1.10) consome SLI/SLO definidos em v1.9 (`slo-engineer`) + RLS/schema definido em v1.8 (`supabase-architect`)
- `toil-auditor` (v1.10) alimenta scoring de OMM Capacidade 3 (Complexidade/Tech Debt) do `omm-auditor` v1.9
- `/sre` (v1.10) é o terceiro orquestrador da família após `/supabase` (v1.8) e `/observabilidade` (v1.9)

**v1.10 é content-only por design** — zero alterações em `src/core/`. Stable API v1.0+ preservada. Mantém budget 6/6 deps.
